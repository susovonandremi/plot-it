services:
  - type: web
    name: plot-ai-backend
    plan: free
    env: docker
    dockerContext: backend
    dockerfilePath: backend/Dockerfile
    envVars:
      - key: PORT
        value: "8000"
